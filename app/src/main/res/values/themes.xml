<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VeilChat" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/veil_primary</item>
        <item name="colorPrimaryVariant">@color/veil_primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/veil_secondary</item>
        <item name="colorSecondaryVariant">@color/veil_accent</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>

    <style name="Theme.VeilChat" parent="Base.Theme.VeilChat" />
    <style name="AlertDialogTheme" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowBackground">@color/veil_background</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@style/DialogAnimation</item>
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <!-- App Lock Button Style -->
    <style name="AppLockButtonStyle" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/veil_surface</item>
        <item name="android:textColor">@color/veil_text_primary</item>
        <item name="cornerRadius">40dp</item>
        <item name="elevation">4dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
    </style>

    <!-- Lock Screen Theme -->
    <style name="Theme.VeilChat.LockScreen" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>