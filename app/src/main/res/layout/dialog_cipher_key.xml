<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Cipher Key"
        android:textColor="@color/veil_text_primary"
        android:textSize="18sp"
        android:textStyle="bold"
        android:gravity="center" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Share this key securely with intended recipients to decrypt cipher messages:"
        android:textColor="@color/veil_text_secondary"
        android:textSize="14sp"
        android:layout_marginTop="16dp"
        android:lineSpacingExtra="4dp" />

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        app:cardBackgroundColor="@color/veil_surface"
        app:cardCornerRadius="8dp"
        app:cardElevation="2dp">

        <TextView
            android:id="@+id/tvCipherKey"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="ABCD1234EFGH5678"
            android:textColor="@color/veil_secondary"
            android:textSize="16sp"
            android:textStyle="bold"
            android:gravity="center"
            android:padding="16dp"
            android:background="?attr/selectableItemBackground"
            android:clickable="true"
            android:focusable="true"
            android:textIsSelectable="true" />

    </com.google.android.material.card.MaterialCardView>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnShareKey"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Share Key"
        android:layout_marginTop="16dp"
        style="@style/Widget.Material3.Button.OutlinedButton" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="⚠️ Without this key, cipher messages cannot be decrypted"
        android:textColor="@color/veil_secondary"
        android:textSize="12sp"
        android:layout_marginTop="16dp"
        android:gravity="center" />

</LinearLayout>