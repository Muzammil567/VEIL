<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:padding="8dp">

    <View
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <com.google.android.material.card.MaterialCardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="48dp"
        app:cardBackgroundColor="@color/veil_secondary"
        app:cardCornerRadius="12dp"
        app:cardElevation="2dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp">

            <!-- Lock Indicator -->
            <View
                android:id="@+id/lockIndicator"
                android:layout_width="4dp"
                android:layout_height="4dp"
                android:background="@color/veil_text_primary"
                android:layout_gravity="end"
                android:layout_marginBottom="4dp"
                android:visibility="gone" />

            <!-- Message Content -->
            <TextView
                android:id="@+id/tvMessageContent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Secret message content"
                android:textColor="@color/white"
                android:textSize="14sp"
                android:maxWidth="240dp" />

            <!-- Self Destruct Indicator -->
            <View
                android:id="@+id/selfDestructIndicator"
                android:layout_width="4dp"
                android:layout_height="4dp"
                android:background="@color/veil_secondary"
                android:layout_gravity="start"
                android:layout_marginTop="4dp"
                android:visibility="gone" />

            <!-- Time and Actions -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="8dp">

                <TextView
                    android:id="@+id/tvMessageTime"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="12:00"
                    android:textColor="#80FFFFFF"
                    android:textSize="10sp" />

                <ImageView
                    android:id="@+id/ivLock"
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:src="@drawable/ic_lock_open"
                    android:layout_marginStart="8dp"
                    android:padding="2dp"
                    android:background="?attr/selectableItemBackgroundBorderless" />

                <ImageView
                    android:id="@+id/ivDelete"
                    android:layout_width="16dp"
                    android:layout_height="16dp"
                    android:src="@drawable/ic_delete"
                    android:layout_marginStart="4dp"
                    android:padding="2dp"
                    android:background="?attr/selectableItemBackgroundBorderless" />

            </LinearLayout>

        </LinearLayout>

    </com.google.android.material.card.MaterialCardView>

</LinearLayout>